\documentclass{standalone}
\usepackage[math,tikz]{forsyde}
\begin{document}
\begin{tikzpicture}[no moc color, 
  type style = \scriptsize\texttt,
  fun/.style={font = \footnotesize}]
  \basic[primitive, moc=sy,f=${f}$] (p1) <0,2.25> {$\MocFun$};
  \basic[primitive, ] (p2) <1,1.5> {$\MocApp$};
  \basic[primitive, ] (p3) <3,0> {$\MocApp$};
  \basic[primitive, ] (p4) <4.5,0> {$\triangleleft$};
  \node[] (in1) at ($(p1)-(1,0)$)    {$\mathcal{S}(\alpha_1)$};
  \node[] (in2) at ($(p2)-(2,0)$)    {$\mathcal{S}(\alpha_2)$};
  \node[] (in3) at ($(p3)-(4,0)$)    {$\mathcal{S}(\alpha_m)$};

  \node[fun,anchor=south west] at ($(p1)!.5!(p2)$) 
    {$\mathcal{S}({\alpha_2 \rightarrow \dotso \rightarrow 
        \alpha_m \rightarrow \beta^n})$};
  \node[fun,anchor=south west] at ($(p2)!.3!(p3)$) 
    {$\mathcal{S}({\dotso \rightarrow \alpha_m 
        \rightarrow \beta^n})$};
  \node[fun,anchor=south west] at ($(p2)!.8!(p3)$) 
    {$\mathcal{S}({\alpha_m \rightarrow \beta^n})$};
  \node[fun] at ($(p3.south)!.5!(p4.south)$) 
    {$\mathcal{S}(\beta^n)$};

  \node[] (ou1) at ($(p4)+(1.3,2.25)$) {$\mathcal{S}(\beta_1)$};
  \node[] (ou2) at ($(p4)+(1.3,1.5)$)  {$\mathcal{S}(\beta_2)$};
  \node[] (ou3) at ($(p4)+(1.3,0)$)    {$\mathcal{S}(\beta_n)$};

  \node[] at ($(in2)!.5!(in3)$) {$\vdots$}; 
  \node[] at ($(ou2)!.5!(ou3)$) {$\vdots$};
  \node[] at ($(p2)!.5!(p3)$)   {$\cdots$};

  \signal[] (in1) -> (p1);
  \signal[] (in2) -> (p2);
  \signal[] (in3) -> (p3);
  \signal[] (p1) -> (p2);
  \signal[] (p2) -> ($(p2)!.3!(p3)$);
  \signal[] ($(p2)!.7!(p3)$) -> (p3);
  \signal[] (p3) -> (p4);
  \signal[] (p4) - (5,2.25); \signal[] (5,2.25) -> (ou1);
  \signal[] (p4) - (5,1.5);  \signal[] (5,1.5) -> (ou2);
  \signal[] (p4) -> (ou3);
  
\end{tikzpicture}
\end{document}


%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
